image: node:latest

stages:
  - build
  - test

dependencies:
  stage: build
  script:
    - cd visual-prog && npm install

test:
  stage: test
  before_script:
    - cd visual-prog
    - npm install --save eslint
  script:
    - echo "Running tests..."
    - npm run test
